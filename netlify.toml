[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

# Single-page app: every route serves index.html (functions under /api are matched first).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
